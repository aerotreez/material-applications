## Exercise 3.2
The express-app.yml workflow triggers a private webhook that triggers a deploy on my Render service that itself pulls and runs the resulting image pushed to my docker hub image repository.

## express app

Access with browser http://localhost:8080
